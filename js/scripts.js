$(document).ready(function(){    $(window).bind('scroll', function(e){        parallaxScroll();        fixedHeader();    });    function toggleMenu() {        $('.toggle').click(function(){            $('.header__navbar').slideToggle();        })    }    toggleMenu();    function parallaxScroll(){        var scrolledY = $(window).scrollTop();        $('.main-carousel__decoration').css('top', ((85 - (scrolledY*0.1)) * -1) + '%');    }    function fixedHeader() {        if ($('header').offset().top > 0 && $(window).width() > 992) {            $('header').addClass('header--scrolled');        } else {            $('header').removeClass('header--scrolled');        }    }    $('.main-carousel').slick({        'fade': true,        prevArrow: $(".pp2"),        nextArrow: $(".nn2")    });    $('.single-carousel').slick({        'fade': true    });    $('.tripple-carousel').slick({        infinite: true,        slidesToShow: 3,        slidesToScroll: 3,        responsive: [            {                breakpoint: 768,                settings: {                    slidesToShow: 2,                    slidesToScroll: 2                }            },            {                breakpoint: 576,                settings: {                    slidesToShow: 1,                    slidesToScroll: 1                }            }        ]    });    $('.desk-item-carousel').slick({      dots: true,      arrows: false,      infinite: true,      speed: 500,      fade: true,      cssEase: 'linear',      autoplay: true,      autoplaySpeed: 4000    });});